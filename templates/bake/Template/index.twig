<?php
/**
 * @var \{{ namespace }}\View\AppView $this
 * @var \{{ entityClass }}[]|\Cake\Collection\CollectionInterface ${{ pluralVar }}
 */
?>
<?php
$this->assign('title', __('{{ pluralHumanName }}'));
$this->Breadcrumbs->add([
    ['title' => __('Home'), 'url' => '/'],
    ['title' => __('List {{ pluralHumanName }}')],
]);
?>

{%- set fields = Bake.filterFields(fields, schema, modelObject, indexColumns, ['binary', 'text']) %}


<div class="card card-primary card-outline">
    <div class="card-header py-2">
        <h2 class="card-title">
            {{ pluralHumanName }}
        </h2>
        <div class="card-tools d-flex gap-4">
            
            <!------ PAGINATION SEARCH ------>
            <!-- REMOVE THIS COMMENT TO ENABLE SEARCH OR DELETE THIS SECTION
            <div class="box-tools gap-2 py-2">
                <?= $this->Form->create(null, ['type' => 'get']); ?>
                <div class="input-group gap-2" style="width: 100%;">

                    <?php //echo $this->SearchLte->searchDay('search_date', ['label'=>'Date', 'value' => $search['search_date']??'']); ?>
                    <?php //echo $this->SearchLte->searchDayRange('search_date', ['label'=>'Date', 
                                    'value' => $search['search_date']??'', 
                                    'value_from' => $search['search_date-from']??'',
                                    'value_to' => $search['search_date-to']??'',
                                    ]); ?>
                    <?php //echo $this->SearchLte->longSelect('search_role', $roles , ['label'=>'Role', 'value' => $search['search_role']??'']); ?>
                    <?php //echo $this->SearchLte->searchSelect('search_role', $roles , ['label'=>'Role', 'value' => $search['search_role']??'']); ?>
                    <?php //echo $this->SearchLte->searchText('search_text', ['type'=>'search','label' => 'Search', 'value' => $search['search_text']??'']); ?>
                    <div class="input-group-append border">
                        <button type="submit" class="btn btn-default"><i class="bi bi-search"></i></button>
                    </div>
                    <div class="input-group-append border">
                        <button type="submit" class="btn btn-default" name="action" value="clear"><i class="bi bi-eraser"></i></button>
                    </div>
                </div>
                <?= $this->Form->end(); ?>
            </div>
            -->
            <!------ PAGINATION SEARCH ------> 
                    
            <?= $this->Paginator->limitControl([], null, [
                'label' => false,
                'class' => 'form-control-sm',
                'templates' => ['inputContainer' => '{{'{{content}}'}}']
            ]); ?>
            <?= $this->Html->link(__('New {{ singularHumanName }}'), ['action' => 'add'], ['class' => 'btn btn-primary text-nowrap', 'style'=>'max-height:2.5rem;']) ?>
        </div>
    </div>
    <!-- /.card-header -->
    <div class="card-body table-responsive p-0">
        <table class="table table-hover text-nowrap">
            <thead>
                <tr>
{% for field in fields %}
                    <th class="lte-sortable"><?= $this->Paginator->sort('{{ field }}') ?></th>
{% endfor %}
                    <th class="actions"><?= __('Actions') ?></th>
                </tr>
            </thead>
            <tbody>
                <?php foreach (${{ pluralVar }} as ${{ singularVar }}) : ?>
                    <tr>
{% for field in fields %}
{% set isKey = false %}
{% if associations.BelongsTo %}
{% for alias, details in associations.BelongsTo %}
{% if field == details.foreignKey %}
{% set isKey = true %}
                        <td><?= ${{ singularVar }}->has('{{ details.property }}') ? $this->Html->link(${{ singularVar }}->{{ details.property }}->{{ details.displayField }}, ['controller' => '{{ details.controller }}', 'action' => 'view', ${{ singularVar }}->{{ details.property }}->{{ details.primaryKey[0] }}]) : '' ?></td>
{% endif %}
{% endfor %}
{% endif %}
{% if isKey is not same as(true) %}
{% set columnData = Bake.columnData(field, schema) %}
{% if columnData.type in ['integer', 'float', 'decimal', 'biginteger', 'smallinteger', 'tinyinteger'] %}
                        <td><?= $this->Number->format(${{ singularVar }}->{{ field }}) ?></td>
{% elseif columnData.type in ['boolean'] %}
                        <td><?= (${{ singularVar }}->{{ field }}) ? __('Yes') : __('No') ?></td>
{% else %}
                        <td><?= h(${{ singularVar }}->{{ field }}) ?></td>
{% endif %}
{% endif %}
{% endfor %}
{% set pk = '$' ~ singularVar ~ '->' ~ primaryKey[0] %}
                        <td class="actions">
                            <?= $this->Html->link(__('View'), ['action' => 'view', {{ pk|raw }}], ['class' => 'btn btn-xs btn-outline-primary', 'escape' => false]) ?>
                            <?= $this->Html->link(__('Edit'), ['action' => 'edit', {{ pk|raw }}], ['class' => 'btn btn-xs btn-outline-primary', 'escape' => false]) ?>
                            <?= $this->Form->postLink(__('Delete'), ['action' => 'delete', {{ pk|raw }}], ['class' => 'btn btn-xs btn-outline-danger', 'escape' => false, 'confirm' => __('Are you sure you want to delete # {0}?', {{ pk|raw }})]) ?>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    </div>
    <!-- /.card-body -->

    <div class="card-footer d-flex flex-column flex-md-row">
        <div class="text-muted pe-2">
            <?= $this->Paginator->counter(__('Page {{ '{{' }}page{{ '}}' }} of {{ '{{' }}pages{{ '}}' }}, showing {{ '{{' }}current{{ '}}' }} record(s) out of {{ '{{' }}count{{ '}}' }} total')) ?>
        </div>
        <ul class="pagination pagination-sm mb-0 gap-2">
            <?= $this->Paginator->first('<i class="bi bi-skip-start-fill"></i>', ['escape' => false]) ?>
            <?= $this->Paginator->prev('<i class="bi bi-caret-left-fill"></i>', ['escape' => false]) ?>
            <?= $this->Paginator->numbers() ?>
            <?= $this->Paginator->next('<i class="bi bi-caret-right-fill"></i>', ['escape' => false]) ?>
            <?= $this->Paginator->last('<i class="bi bi-skip-end-fill"></i>', ['escape' => false]) ?>
        </ul>
    </div>
    <!-- /.card-footer -->
</div>
